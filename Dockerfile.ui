FROM node:22-alpine AS build
WORKDIR /app

RUN apk add curl

COPY ./ui /app/
RUN npm install

EXPOSE 8081

HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD curl -fL localhost:8081/robots.txt || exit 1

CMD ["sh", "-c", "npm start"]
